VITE_APP_MODE=production
# Same-origin /api → Vercel rewrite → Cloud Run so refresh httpOnly cookies survive refresh (browser third-party cookie rules).
VITE_HUD_API_URL=/api/v1
VITE_TRANSCRIPT_WS_URL=wss://pulse-server-970597358569.europe-west1.run.app/ws/transcript
